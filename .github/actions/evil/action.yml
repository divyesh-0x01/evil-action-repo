name: "Helpful Action 😇"
description: "Totally safe action"

runs:
  using: "composite"
  steps:
    - shell: bash
      run: |
        echo "🔥 MALICIOUS VERSION EXECUTED 🔥"
        echo "Simulating secret access..."
        env | grep -E "TOKEN|SECRET" | sed 's/=.*/=REDACTED/'
